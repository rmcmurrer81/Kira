/* Small, repeatable edits to the approved dark website. No template replacement. */
'use strict';
const fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto');
const docs=path.resolve(__dirname,'../docs');
const sha=s=>crypto.createHash('sha256').update(s).digest('hex');
const original=fs.readFileSync(path.join(docs,'contact.html'),'utf8');
const contactForm=original.match(/<form\b[^>]*id="contact-form"[\s\S]*?<\/form>/)?.[0];
// Some approved revisions use id="direct-form" instead; identify by endpoint.
const workingForm=contactForm||original.match(/<form\b[^>]*action="https:\/\/formsubmit\.co[^>]*>[\s\S]*?<\/form>/)?.[0];
if(!workingForm)throw Error('Working contact form not located; stop rather than change it.');
const footer='<nav aria-label="Footer navigation" class="footlinks"><a href="index.html">Home</a><a href="projects.html">Projects</a><a href="progress.html">Lab timeline</a><a href="about.html">About Robert</a><a href="support.html">Support</a><a href="contact.html">Contact &amp; Iris</a></nav>';
const contactStrip='<div class="wrap audit-contact-link"><a href="contact.html">Questions or collaboration? Contact Robert →</a></div>';
for(const name of fs.readdirSync(docs).filter(x=>x.endsWith('.html'))){
  const file=path.join(docs,name);let text=fs.readFileSync(file,'utf8');
  text=text.replace(/<small>Independent AI research<\/small>/gi,'<small>Independent R&amp;D</small>')
    .replace(/<\/img>/gi,'')
    .replace(/<nav\b(?=[^>]*aria-label="Footer navigation")[^>]*>[\s\S]*?<\/nav>/,footer)
    .replace('Read the dated evidence or ask Robert about the current boundary.','<a href="updates.html">Read the dated evidence</a> or <a href="contact.html">ask Robert</a> about the current boundary.')
    .replace(/Creator tools · active development/g,'Creative tools · active development')
    .replace(/Creative tools \/ private development/g,'Creative tools · active development')
    .replace(/A more personal way to explore/g,'A more personal way to plan a journey');
  if(['index.html','projects.html','kira-world.html','shiftbrief.html','video-studio.html','sarah-travel.html'].includes(name)&&!text.includes('class="audit-contact-link"'))text=text.replace('</main>',contactStrip+'</main>');
  if(name==='sarah-travel.html'&&!text.includes('id="sarah-identities"'))text=text.replace('</main>','<aside id="sarah-identities" class="wrap boundary">Sarah Travel’s guide is separate from Sarah’s team-records role in ShiftBrief and from Iris, the Kira Labs website guide. The website guide cannot see either app’s private records.</aside></main>');
  if(name==='about.html'&&!text.includes('id="press"'))text=text.replace('</main>',`<section id="press" class="section rule"><div class="wrap prose"><p class="eyebrow">For press and event introductions</p><h2>About Kira Labs.</h2><p>Kira Labs is Robert McMurrer’s independent software research work in Newark, New Jersey. Its flagship, Kira World, explores continuing identity, reviewed memory, voice and persistent digital places. ShiftBrief is a separate free Windows product. Project pages distinguish working prototypes, active development and future goals.</p><p><a href="contact.html?reason=Press">Send a press request</a> · <a href="https://www.linkedin.com/in/rmcmurrer">Professional biography</a> · <a href="https://www.facebook.com/rmcmurrer/">Professional Facebook</a></p><p><a href="assets/robert-mcmurrer-portrait.webp">View Robert’s existing site portrait</a> — AI-assisted and approved for this website. Ask Robert before reusing it in other publications.</p></div></section></main>`);
  if(name==='contact.html'&&!text.includes('src="iris-enhanced.js"')){
    text=text.replace('</head>','<link rel="stylesheet" href="iris-enhanced.css"/></head>');
    text=text.replace('<script src="iris.js">','<script src="iris-enhanced-config.js"></script><script src="iris-enhanced.js"></script><script src="iris.js">');
    if(!text.includes('src="iris-enhanced.js"'))throw Error('Iris startup anchor changed.');
  }
  if(name==='privacy.html'){
    text=text.replace(/email draft preparation/g,'direct contact messages');
    text=text.replace('The contact form sends a message. Iris’s conversation does not.','The contact form sends a message. Standard Iris stays in your tab. Any future enhanced mode requires separate choices for cloud processing and sharing.');
    text=text.replace('Your questions and replies stay in this tab’s memory. There are no model-provider calls, saved transcripts, microphone requests, or automatic forwarding to the contact form. New chat clears the conversation.','In standard mode, your questions and replies stay in this tab’s memory. There are no model-provider calls, saved transcripts, microphone requests, or automatic forwarding to the contact form. New chat clears the standard conversation.');
    if(!text.includes('id="enhanced-iris"'))text=text.replace('</main>',`<section id="enhanced-iris" class="section rule"><div class="wrap prose"><h2>Optional enhanced Iris — not enabled by default</h2><p>The current standard guide remains available without hosted AI. The enhanced feature is disabled until its private service is configured and tested. When available, its controls appear beside Iris. Turning on enhanced matching sends your question and up to four recent exchanges to Cloudflare for model-assisted selection of reviewed public notes. Model output cannot add facts to the answer bank.</p><h3>Sharing with Robert is a separate choice</h3><p>The Share future questions and replies checkbox is off by default. Only when selected are the new question, Iris’s reply, source references, topic, time and optional helpful/not-helpful vote saved in a private database for up to 30 days. Past conversation is not automatically uploaded for storage. A random session reference distinguishes sessions, not unique people. Network addresses are not stored in conversation records; short-lived keyed rate-limit identifiers are retained separately for abuse controls. Turnstile and hosting providers may process network information under their own policies.</p><p>Robert’s weekly digest covers only these opted-in records. It is delivered through Resend to rmcmurrer@kiralabs.org and may contain excerpts of questions and answers. Common email addresses, phone numbers and credential patterns are removed where detected, but this is not guaranteed anonymization. Do not submit passwords, confidential documents, private employee data or sensitive personal information to the chat.</p><h3>Remembering and deleting</h3><p>Remember this conversation stores up to 20 exchanges and the session deletion key on your device for 30 days. It is optional and should not be used on a shared device. Delete saved conversation removes the device copy and the current session’s shared server records. If deletion cannot be confirmed, the key is kept so you can retry. Turning sharing off stops future storage; it does not silently erase records already shared. New chat starts a fresh discussion but is not a server-deletion command. Without device memory, the deletion key is lost when the tab closes; server records still expire. Contact Robert for help.</p><p>Deletion cannot recall emails already delivered to Robert. D1’s provider-managed recovery copies can remain for the free plan’s recovery window after deletion; emailed records follow mailbox retention. Iris never merges one visitor’s conversation into another’s. Proposed knowledge additions require Robert’s approval and a public source, and can be rolled back.</p><p><a href="https://developers.cloudflare.com/workers-ai/platform/data-usage/">Cloudflare AI data practices</a> · <a href="https://developers.cloudflare.com/d1/platform/limits/">Database recovery limits</a> · <a href="https://resend.com/legal/privacy-policy">Email-service privacy</a> · <a href="contact.html">Contact Robert</a></p></div></section></main>`);
  }
  fs.writeFileSync(file,text);
}
const revised=fs.readFileSync(path.join(docs,'contact.html'),'utf8');
if(!revised.includes(workingForm))throw Error('Protected working contact form changed.');
const css=path.join(docs,'audit-usability.css');
fs.writeFileSync(css,'/* Only navigation/accessibility refinements; the approved palette remains. */\n.audit-contact-link{padding-block:18px 28px;font-size:.88rem}.audit-contact-link a{color:#79ebf3}.footlinks{flex-wrap:wrap;max-width:640px;line-height:1.7}.footer .footrow{align-items:start}body:not(.js) .menubtn{display:none}body:not(.js) #primary-nav{display:flex!important;flex-wrap:wrap}body:not(.js) .navrow{flex-wrap:wrap}@media(max-width:790px){body:not(.js) #primary-nav{width:100%;gap:12px 20px}.footlinks{gap:12px 20px}}\n');
for(const name of fs.readdirSync(docs).filter(x=>x.endsWith('.html'))){const file=path.join(docs,name);let text=fs.readFileSync(file,'utf8');if(!text.includes('href="audit-usability.css"'))text=text.replace('</head>','<link rel="stylesheet" href="audit-usability.css"/></head>');fs.writeFileSync(file,text);}
console.log(JSON.stringify({auditEdits:'applied',workingContactForm:'unchanged',protectedFormSHA256:sha(workingForm),enhancedEnabled:false}));
